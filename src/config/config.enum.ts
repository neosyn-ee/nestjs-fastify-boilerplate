export enum ConfigKey {
  App = 'APP',
  Db = 'DB',
  Loki = 'LOKI',
}

export enum Environment {
  Local = 'local',
  Development = 'development',
  Preprod = 'preprod',
  Production = 'production',
}
